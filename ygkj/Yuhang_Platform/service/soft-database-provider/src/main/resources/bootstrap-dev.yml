
server:
  port: 8899
  undertow:
    # 设置IO线程数, 它主要执行非阻塞的任务,它们会负责多个连接, 默认设置每个CPU核心一个线程
    io-threads: 4
    # 阻塞任务线程池, 当执行类似servlet请求阻塞操作, undertow会从这个线程池中取得线程,它的值设置取决于系统的负载
    worker-threads: 20
    # 以下的配置会影响buffer,这些buffer会用于服务器连接的IO操作,有点类似netty的池化内存管理
    # 每块buffer的空间大小,越小的空间被利用越充分
    buffer-size: 1024
    # 是否分配的直接内存
    direct-buffers: true

spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: DEFAULT_GROUP
  application:
    name: soft-database
  datasource:
    druid:
      url: jdbc:mysql://192.168.2.130:3306/yh_meta_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&useAffectedRows=true
      username: yh_platform
      password: WiVlEJ4BoWaP1OEL
      driver-class-name: com.mysql.jdbc.Driver
      keep-alive: true
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    database: 0
    host: 192.168.2.130
    port: 6379
    password: xB239MCfg3321Pq002
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0



  jackson:
    date-format: yyyy-MM-dd HH:mm:ss



#rocketmq:
#  name-server: 192.168.2.188:9876   #rocketMQ服务的地址
#  producer:
#    group: soft-data # 生产者组


## 日志输出配置
logging:
  file:
    name: ${logging.file.path}/${spring.application.name}.log
    path: log
  level:
    com.ygkj: info

jwt:
  secretKey: 78944878877848fg)
  accessTokenExpireTime: PT2H
  refreshTokenExpireTime: PT8H
  refreshTokenExpireAppTime: P30D
  issuer: yingxue.com

seata:
  application-id: ${spring.application.name} # Seata 应用编号，默认为 ${spring.application.name}
  tx-service-group: ${spring.application.name} # Seata 事务组编号
  # 服务配置项，对应 ServiceProperties 类
  service:
    # 虚拟组和分组的映射
    vgroup-mapping:
#      multi-datasource-service-group: default
      soft-database: default #为当前应用指定分组
    grouplist:
      default: 127.0.0.1:8091 #指定分组所在server地址



schema:
  prefix: yh_

