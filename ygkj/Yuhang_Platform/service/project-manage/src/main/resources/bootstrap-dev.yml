server:
  port: 8100

spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: DEFAULT_GROUP
  application:
    name: project-manage
  datasource:
    druid:
      url: jdbc:mysql://192.168.2.130:3306/yh_basic_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&useAffectedRows=true
      username: yh_platform
      password: WiVlEJ4BoWaP1OEL
      driver-class-name: com.mysql.jdbc.Driver
      keep-alive: true
    type: com.alibaba.druid.pool.DruidDataSource
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss



## 日志输出配置
logging:
  file:
    name: ${logging.file.path}/${spring.application.name}.log
    path: log
  level:
    com.ygkj: info

jwt:
  secretKey: 78944878877848fg)
  accessTokenExpireTime: PT2H
  refreshTokenExpireTime: PT8H
  refreshTokenExpireAppTime: P30D
  issuer: yingxue.com

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    call-setters-on-nulls: true
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl


