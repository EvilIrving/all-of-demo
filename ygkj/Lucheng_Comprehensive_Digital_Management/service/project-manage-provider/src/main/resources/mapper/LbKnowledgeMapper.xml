<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ygkj.water.project.mapper.LbKnowledgeMapper">
    <select id="loadPage" resultType="com.ygkj.project.vo.response.LbKnowledgeRespVo">
        SELECT tbl.*,file.file_name appendixName,file.file_url appendixUrl FROM `lb_knowledge` tbl
        LEFT JOIN sys_file file on tbl.appendix = file.id
        where tbl.del_flag = 0
        <if test ='null != id'>
            and tbl.id = #{id}
        </if>
        <if test ='null != canonicalName and "" != canonicalName'>
            and tbl.canonical_name  LIKE CONCAT('%',#{canonicalName},'%')
        </if>
    </select>
</mapper>