
server:
  port: 8984
  undertow:
    # 设置IO线程数, 它主要执行非阻塞的任务,它们会负责多个连接, 默认设置每个CPU核心一个线程
    io-threads: 4
    # 阻塞任务线程池, 当执行类似servlet请求阻塞操作, undertow会从这个线程池中取得线程,它的值设置取决于系统的负载
    worker-threads: 20
    # 以下的配置会影响buffer,这些buffer会用于服务器连接的IO操作,有点类似netty的池化内存管理
    # 每块buffer的空间大小,越小的空间被利用越充分
    buffer-size: 1024
    # 是否分配的直接内存
    direct-buffers: true
  compression:
    enabled: true
    mime-types: application/javascript,text/css,application/json,application/xml,text/html,text/xml,text/plain

dubbo:
  application:
    qos-enable: false
    id: meteorological-visualization
  scan:
    base-packages: com.ygkj.visualization.rpc
  protocol:
    port: 20881
    name: dubbo
  registry:
    address: spring-cloud://127.0.0.1
    check: false
  consumer:
    check: false
  cloud:
    subscribed-services: meteorological-visualization




spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
  application:
    name: meteorological-visualization
  autoconfigure:
    exclude: com.alibaba.druid.spring.boot.autoconfigure.DruidDataSourceAutoConfigure
  datasource:
    dynamic:
      primary: basic
      strict: false
      datasource:
        basic:
          url: jdbc:mysql://192.168.2.130:3306/lc_basic_db?useUnicode=true&characterEncoding=utf-8&useSSL=false
          username: lc_plt
          password: GXvJaDjeuyPCZSKO
          driver-class-name: com.mysql.jdbc.Driver
        business:
          url: jdbc:mysql://192.168.2.130:3306/lc_business_db?useUnicode=true&characterEncoding=utf-8&useSSL=false
          username: lc_plt
          password: GXvJaDjeuyPCZSKO
          driver-class-name: com.mysql.jdbc.Driver
      initial-size: 20
      min-idle: 10
      max-active: 30
      max-wait: 30000
      min-evictable-idle-time-millis: 180000
  AbstractType: com.alibaba.druid.pool.DruidDataSource
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  redis:
    timeout: PT10S
    jedis:
      pool:
        max-idle: 6
        max-active: 32
        max-wait: 100
        min-idle: 4
    database: 5
    port: 20006
    host: 112.17.127.75
    password: xB239MCfg3321Pq002

## 日志输出配置
logging:
  file:
    name: ${logging.file.path}/${spring.application.name}.log
    path: log
  level:
    com.ygkj: debug

jwt:
  secretKey: 78944878877848fg)
  accessTokenExpireTime: PT2H
  refreshTokenExpireTime: PT8H
  refreshTokenExpireAppTime: P30D
  issuer: yingxue.com

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    call-setters-on-nulls: true
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


weather:
  syn-rain-path: C:\\develop\\rain-data
  syn-five-km-rain-path: C:\\develop\\5km-rain-data\\currentData


