<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ygkj.digitization.mapper.SysDeptMapper">
    <select id="selectDeptNameAdcdByDeptId" resultType="map" >
        select adcd,name from sys_dept where id=#{deptId}
    </select>
</mapper>